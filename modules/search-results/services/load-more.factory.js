/*
(function () {
    angular
        .module('atlasSearchResults')
        .factory('loadMore', loadMoreFactory);

    loadMoreFactory.$inject = ['api'];

    function loadMoreFactory (api) {
        return {
            next: next
        };

        function next (endpoint) {

        }
    }
})();
*/